spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/reports-realm
          #remove /auth/ in keycloack v17+
          #issuer-uri: http://localhost:8080/auth/realms/reports-realm